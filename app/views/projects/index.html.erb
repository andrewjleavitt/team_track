<h1>Projects</h1>

<%= link_to "New Project", new_project_path %>

<ul>
  <% @projects.each do |project| %>
    <li class="project"><%= link_to project.name, project %></li>
    <li class="team"><%= project.team.name %></li>
  <% end %>
</ul>
