<%= form_with model: @member do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name, id: 'Name' %>
    <%= f.label :team %>
    <%= f.collection_select(:team, @teams, :name, :name, include_blank: true )%>
  </p>
  <%= f.submit "Save" %>
<% end %>
